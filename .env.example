# Added automatically when a Neon database is connected through Vercel.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional: sends an email notification after the database save succeeds.
WEB3FORMS_ACCESS_KEY=
